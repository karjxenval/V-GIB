# Contributing

This is a research-code repository. Contributions should preserve reproducibility, clear experiment configuration, and explicit output locations.

Before opening a pull request:

1. Run `python -m py_compile` on edited scripts.
2. Add or update a smoke-test command in the README when adding a new experiment.
3. Avoid committing datasets, large logs, model checkpoints, or generated figures unless they are intentionally small examples.
